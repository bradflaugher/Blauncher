<?xml version="1.0" encoding="utf-8"?>
<!-- Filled disc behind the send arrow: the one solid element on the home screen, so it reads as
     the action to take. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="?attr/primaryColor" />
</shape>
